# 故障知识库系统需求规格说明书

> **本文档定位：系统设计原理、架构决策、技术实现细节、约束条件**。
> 每次任务执行过程中涉及设计原理或架构决策的变更，更新本文档。

---

## 1. 项目概述

### 1.1 项目目标

构建一个基于 285 份输电线路故障报告的知识库问答系统，命名为 **故障知识库**。系统核心能力是回答关于线路故障的精准查询，包括：

- 单线路故障详情
- 单线路指定杆塔故障详情
- 多线路故障统计
- 排序统计
- 对比统计
- 故障类型现象归纳

答案必须精确到：日期、故障线路、电压等级、故障杆塔号、故障类型、精确时间戳（秒/毫秒，如有）、简要情况概述，并标注来源文件超链接。

### 1.2 项目范围

- **数据源**：`/Users/yfzx/Desktop/故障知识库/故障报告/` 下的 285 份报告
  - PDF：220 份
  - DOC：43 份
  - DOCX：22 份（含 7 份由 WPS 转换而来，原件备份于 `故障报告_WPS备份/`）
- **时间跨度**：2012 年 ~ 2026 年（以 2026 年为主，202 份）
- **地域范围**：中国大陆各省级电网公司（国网 XX 电力）
- **输出目标**：支持 CLI 和简单 Web 前端测试

### 1.3 与 Forecast 项目的关系

- **复用**：bge-m3 Embedding 模型、gemma-4-31B-it 大模型、OpenAI Compatible API 调用方式、Qdrant 本地向量库、Skill 化 Prompt 架构思想。
- **不照搬**：所有源码、Skill、Prompt 均针对故障报告领域重新设计和实现。

---

## 2. 查询分类与 Skill 定义

### 2.1 Skill 总览

| Skill | 名称 | 优先级 | 说明 |
|-------|------|--------|------|
| `single_line_stats` | 单线路统计 | 核心 | 查询某条线路在某时段内的全部故障事件 |
| `single_tower_stats` | 单杆塔统计 | 核心 | 查询某条线路指定杆塔在某时段内的故障事件 |
| `multi_line_stats` | 多线路统计 | 核心 | 按电压等级、省份、故障类型等条件统计多条线路 |
| `ranking_stats` | 排序统计 | 扩展 | Top-N 线路、最频繁月份等排序类查询 |
| `compare_stats` | 对比统计 | 扩展 | 不同维度（省份/年份/灾害类型/线路）对比 |
| `fault_type_stats` | 故障类型归纳 | 扩展 | 某类故障的原因、现象、伴随参数简要归纳 |
| `default` | 通用兜底 | 必需 | 不匹配任何 Skill 时回退到通用问答 |

### 2.2 `single_line_stats`

**目标**：回答单条线路在某时段内的全部故障事件。

**示例问题**：
- "泰吴线2025年雷击故障情况"
- "廻津5263线2026年7月故障"
- "锦苏线历年故障情况"（历年 = 不加年份过滤，枚举全部年份）
- "复奉线历年雷击故障情况"（历年 + 故障类型组合）
- "泰吴线2026年第一季度雷击故障情况"（季度过滤）
- "雅湖线2023年至2025年情况"（年份范围，兼容"2023-2025年/2023年-2025年/2023至2025年/2023~2025年"等写法）
- "雅湖线2025年4至7月情况"（月份范围，兼容"3-8月/4月到7月/4~7月"等写法）

**输出要求**：
- 按故障时间升序分点回答
- 每条包含：时间（精确到秒，正文有毫秒则到毫秒）、线路名称、电压等级、故障杆塔号、故障类型、简要概述、来源文件超链接
- 同一事件内多次跳闸需列出每次跳闸时间
- 绕击/反击/雷害统一归并为"雷击"（同义词规则），细分区分由事件卡"雷击细分"字段承担，不进入 fault_type 标签体系

**检索策略**：
- 元数据 must 过滤：`line`、`year`（历年查询不过滤年份）
- 可选 must 过滤：`fault_type`（绕击/反击查询经同义词归一按"雷击"过滤）、`quarter`、`month`、`date`
- 历年、多年份范围等枚举类查询：元数据过滤全量拉取事件卡（summary 块），上限 `ALL_YEARS_MAX_CHUNKS=40`，清单完整性优先
- 聚焦类查询：向量召回 summary + detail 混合块

### 2.3 `single_tower_stats`

**目标**：回答单条线路指定杆塔的故障事件。

**示例问题**：
- "泰吴线106号塔2025年雷击故障"
- "廻津5263线2026年7月106号杆塔情况"
- "雅湖线2911号塔2023年至2025年情况"（年份范围 + 杆塔组合）

**输出要求**：
- 按时间升序分点回答
- 杆塔号统一输出（如 "106号"）
- 范围查询输出范围（如 "106-110号"）
- "106号塔附近放电"等表述，故障杆塔认定为 106 号

**检索策略**：
- 元数据 must 过滤：`line`、`year`
- 杆塔号**不进入检索层 must 过滤**，作为后处理高亮字段
- LLM 在召回候选块中确认杆塔号

**边界规则**：
- 查询杆塔号必须携带线路名，否则提示用户补充线路名
- 不同线路可能存在相同杆塔号，但不允许无线路名的杆塔查询

### 2.4 `multi_line_stats`

**目标**：按条件统计多条线路的故障情况。本质即运维单位统计（省份 = 运维单位，不单独设"运维单位统计"查询类）。

**示例问题**：
- "2025年湖北500kV线路雷击故障"
- "2026年7月国网江苏电力异物短路故障"
- "2025年6月10日全国500kV线路情况"（日期过滤）
- "2025年第一季度湖北1000kV线路情况"（季度过滤）

**输出要求**：
- 列出符合条件的线路清单
- 每条线路给出故障次数、每次故障时间、杆塔号、类型、概述
- 支持按省份、电压等级、故障类型组合过滤
- 800kV 与 ±800kV 归为一类（电压归一化后等价）

**检索策略**：
- 不按设备索引枚举全部线路
- 直接按 `year` + `province` + `voltage` + `fault_type` + `quarter`/`month`/`date` 检索
- 程序化预聚合：全量扫描事件卡元数据生成【精确统计】表（按线路分布，无召回上限），计数/排名以统计表为准，卡片仅提供细节与来源

### 2.5 `ranking_stats`

**目标**：排序类统计查询。

**示例问题**：
- "2025年湖北遭受雷击故障最多的前三条线路"
- "2026年XX线路山火最频繁的月份是哪几个"

**输出要求**：
- 输出排序结果
- 每条结果附带具体故障时间（不要求列出全部事件，但需给出关键示例）

**实现方式**：
- 排名依据【精确统计】表（全量元数据聚合，不受召回条数限制），禁止 LLM 数卡片计数
- 统计维度自适应：查询锁定单线路时按**月份**分组（最频繁月份场景），否则按**线路**分组（Top-N 场景）
- Top-N 场景对榜单线路（含并列）补拉事件卡，保证有细节和来源

### 2.6 `compare_stats`

**目标**：不同维度对比。

**对比维度**：
1. 同一省份不同灾害对比
2. 同一省份两年事故对比
3. 不同省份同一灾害对比
4. 同一线路不同灾害对比
5. 同一线路两年事故对比

**示例问题**：
- "2025年湖北雷击和风偏故障哪个更多"
- "2024年和2025年湖北山火故障数量对比"

**输出要求**：
- 对比结果以表格或分章节形式呈现
- 每侧计数来自该侧独立的【精确统计】表（全量元数据聚合）
- 每侧至少列出一个代表性事件及来源文件超链接
- 引用来源文件

**实现方式**：
- 对比问题拆分为两侧子查询，分别元数据过滤 + 聚合
- 合并两侧召回块（按 report_id 去重），生成层做对比汇总

### 2.7 `fault_type_stats`

**目标**：某类故障的现象、原因、伴随参数简要归纳。

**示例问题**：
- "雷击故障通常伴随多大雷电流？"
- "风偏故障一般发生在什么气象条件下？"

**输出要求**：
- LLM 基于召回块简要回答
- 不做精确数值统计
- 答案保守，不确定时标注

### 2.8 `default`

**目标**：兜底通用问答。

**示例问题**：
- "这份报告的主要内容是什么"
- "江苏有哪些线路发生过山火"

**实现方式**：
- 直接向量检索
- 通用 Prompt 生成回答

---

## 3. 元数据 Schema

每个文档块必须附加完整元数据：

```python
{
    "report_id": str,           # 文件路径哈希，用于去重
    "report_name": str,         # 原始文件名
    "report_path": str,         # 原始文件绝对路径
    "file_server_url": str,     # 文件服务器访问地址
    "year": int,                # 故障发生年份
    "quarter": int,             # 季度（1-4）
    "month": int,               # 月份（1-12）
    "date": str,                # 日期（YYYY-MM-DD）
    "province": str,            # 省份（统一省级）
    "operator": str,            # 运维单位（统一到省级）
    "line": str,                # 线路名称
    "voltage": str,             # 电压等级（归一化，如 500kV）
    "towers": List[str],        # 杆塔号列表（从正文提取）
    "fault_type": str,          # 故障类型
    "fault_types": List[str],   # 报告中涉及的全部故障类型
    "chunk_type": str,          # 块类型：event / summary / detail_0~N（详情块按序号后缀）
    "event_id": str,            # 故障事件唯一标识
}
```

### 3.1 字段说明

#### report_id

基于文件绝对路径计算 SHA256，取前 16 位。索引时强制 `os.path.abspath(file_path)` 规范化路径，避免同一文件因路径格式不同被重复索引。

#### year / quarter / month / date

从文件名和正文双重提取：
- 文件名优先（如 `...2026年6月30日...`）
- 正文作为补充和校验

#### province

从文件名中的运维单位提取，统一映射到省级：
- "国网湖北电力" → 湖北
- "湖北超高压公司" → 湖北
- "国网江苏电力" / "国网江苏公司" → 江苏

#### voltage

归一化规则：
- "±800千伏" / "±800kV" / "800kV" / "800千伏" → 800kV
- "500kV" / "500千伏" / "500千伏" → 500kV
- "1000kV" / "1000千伏" / "特高压" → 1000kV
- 输出统一使用 `数字kV` 格式

输入/输出规范（2026-08-05 用户强调，必须遵守）：
- **输入宽容**：用户问题中的电压单位 kV、Kv、kv、KV 及中文"千伏"均必须识别，不得严格要求用户输入的大小写。
- **输出严格**：系统的一切输出（回答正文、反问提示、示例问法）必须使用标准 `数字kV` 格式（小写 k、大写 V）。

#### line

线路名称，如 "武宗Ⅰ线"、"廻津5263线"。

规范化（`canonicalize_line_name`，入库与查询双侧统一）：
- "XX直流" / "XX直流联络线" → "XX线"（如 锦苏直流 → 锦苏线；金永直流联络线 → 金永线）
- "XX线极Ⅱ线" / "XX直流I线" 等极号后缀剥离（仅限直流极号，不误伤"艳牌I线"等真名）

家族查询（`line_stem` + `expand_line_variants`，仅查询侧扩展、不改存储名）：
- 查询裸名（荆潇线/渔兴线/江莲线）→ 命中同族全部编号变体（荆潇I线+荆潇II线、渔兴一线+渔兴一二线+渔兴三回线、江莲一线+江莲二线）
- 查询精确变体（荆潇II线）→ 只回该变体
- 编号后缀模式：罗马数字（ⅠⅡⅢIV）、中文数字含"回"（一/一二/三回）、阿拉伯数字含"号"（1号/5253）、数字字母混合（5P16/5K73）
- 回答按事件卡上的具体线路名区分变体

#### towers

杆塔号列表，从正文中正则提取。支持格式：
- "#106"
- "106#"
- "106号塔"
- "106号铁塔"
- "106-110号塔" → 存储为 "106-110号"
- "106号塔附近放电" → 提取为 "106号"

#### fault_type / fault_types

故障类型主要分类：
- 雷击
- 异物短路
- 风偏
- 山火
- 鸟害 / 鸟粪
- 舞动
- 冰害 / 冰闪
- 脱冰跳跃
- 雪闪
- 断线
- 外力破坏
- 其他

`fault_type` 为单一主类型，`fault_types` 为报告中涉及的全部类型。

绕击/反击不作为独立 fault_type 标签（2026-08-04 设计变更）：二者是雷击的同级细分，但历史上绕击标签来自文件名偶然用词（仅 5 份）、反击无标签，无法构成完整分类。索引侧与查询侧统一经同义词规则归一为"雷击"（绕击/反击/雷害 → 雷击），细分区分由事件卡"雷击细分"字段承担，回答时按卡标注输出"雷击（绕击）/雷击（反击）"。

---

## 4. 系统架构

### 4.1 四层优化策略

借鉴 Forecast 项目思路，针对故障报告特点设计四层优化：

| 层级 | 策略 | 说明 |
|------|------|------|
| L1 解析层 | 多格式统一解析 | PDF 用 pdfplumber，DOCX 用 python-docx，DOC/WPS 用 LibreOffice soffice |
| L2 元数据层 | 文件名 + 正文双重提取 | 日期、线路、电压、省份、故障类型 |
| L3 检索层 | 元数据预筛选 + 向量召回 | 按时间、线路、电压、故障类型 must 过滤，再语义召回 |
| L4 生成层 | Skill 化 Prompt + 来源标注 | 按 Skill 选择模板，生成带文件超链接的回答 |

### 4.2 分块策略

- 每个故障报告对应一个或多个块
- **事件卡（`summary` 块）**：索引时对每份报告调用 LLM 抽取 7 字段结构化卡片（故障时间/故障杆塔/雷击细分/故障原因/故障时天气/重合闸情况/概述），"短则照抄原文、长则浓缩"；提取失败降级为旧式文本截断并记 WARN 日志
- **详情块**：跳闸时间、保护措施、气象条件等作为 `detail` 块（按序号后缀 detail_0~N）
- 块大小不超过 4000 字符，避免超长
- **正文时间优先**：`apply_card_datetime` 以事件卡"故障时间"覆盖元数据 year/month/quarter/date（正文与文件名冲突时以正文为准）

### 4.3 去重规则

- 同一文件（按 report_id）不重复索引
- 业务去重：同一天 + 同一线路 + 同类型 + 同杆塔 = 一次故障事件
- 同一事件内多次跳闸保留每次时间

---

## 5. 解析策略

### 5.1 PDF 解析

- 工具：pdfplumber 0.11.9
- 保留表格结构
- 提取正文和表格文本

### 5.2 DOCX 解析

- 工具：python-docx
- 提取段落文本，保留标题层级

### 5.3 DOC / WPS 解析

- 工具：LibreOffice `soffice --headless --convert-to txt:Text`
- 因为 macOS textutil 无法处理旧二进制格式
- 先转换到临时 txt，再读取

### 5.4 元数据提取流程

1. 从文件名提取：省份、电压、线路、日期、故障类型
2. 从正文标题/首段验证和补充
3. 从正文提取杆塔号、跳闸时间
4. 归一化所有字段

---

## 6. 检索策略

### 6.1 元数据预筛选

根据 Skill 和查询参数构建 Qdrant `must` 过滤条件：

| 查询参数 | 过滤字段 | 说明 |
|----------|----------|------|
| 年份 | `year` | 单年、范围（2023-2025/2023年至2025年等，展开为年份列表）或历年（不过滤） |
| 季度 | `quarter` | 必须带年份 |
| 月份 | `month` | 单点或范围（3-8月/4至7月等，展开为月份列表），必须带年份 |
| 日期 | `date` | 精确日期 |
| 线路 | `line` | must |
| 杆塔号 | 不进入 must | 后处理高亮 |
| 故障类型 | `fault_type` | must |
| 电压等级 | `voltage` | must |
| 省份 | `province` | must |

### 6.2 向量召回

- Embedding：bge-m3，1024 维
- 距离度量：Cosine
- Top-K：默认 10

### 6.3 关联块拉取

根据 Skill 动态拉取关联块：
- `single_line_stats`：event + detail + summary
- `single_tower_stats`：event + detail
- `multi_line_stats`：event + summary
- `ranking_stats` / `compare_stats` / `fault_type_stats`：基于 multi_line 结果聚合

### 6.4 语义路由与开放词汇掩码

- 路由决策 100% 由 Embedding 相似度完成，不使用硬编码正则兜底（用户问题表述不可枚举）
- 向量化前对**例句和问题双侧**执行 `mask_line_tower`：线路名 → "某线"，杆塔号（含区间、#号、杆塔/塔杆各种后缀）→ "某号塔"
- 相似度比较句式结构/意图而非词面词汇（188+ 线路名无法枚举进例句）
- 路由相似度低于阈值（0.75）时回退 `default`；新句式通过向对应 Skill 的 `examples.txt` 补充例句提升覆盖率

### 6.5 程序化预聚合（方案B）

列举类查询（multi_line / ranking / compare）的计数与排名不依赖 LLM 数卡片：
- `aggregate_summary_counts` 全量扫描命中事件卡的元数据分组计数，无召回条数上限、不调 LLM API、本地毫秒级
- 生成【精确统计】表注入 Prompt 置于卡片之前；计数/排名必须以统计表为准，卡片仅提供事件细节与来源
- 分组维度：默认按线路；查询锁定单线路时按月份（"最频繁月份"场景）

---

## 7. 生成策略

### 7.1 Skill 化 Prompt

- Prompt 模板存放于 `Project_Skills/prompt-skills/{skill_name}/prompt.txt`
- 每个 Skill 可配置 `SKILL.md` 和 `examples.txt`
- `examples.txt` 用于语义路由训练/匹配

### 7.2 生成要求

- 按时间升序分点回答
- 每条事件独立编号
- 必须包含：时间、线路、电压、杆塔号、故障类型、概述
- 杆塔号缺失时标注 "杆塔号未提及"
- 每次跳闸时间精确到秒/毫秒（如有）
- 每条末尾标注来源文件超链接
- 生成参数：`LLM_MAX_TOKENS=8192`（约 22 条完整事件容量，防大清单截断）、`LLM_TIMEOUT=300`（数据密集型查询首 token 耗时长属可解释行为）

### 7.3 超链接格式

```markdown
[故障报告：国网XX电力500kVXX线202X年X月X日XX故障分析报告.pdf](http://{file_server_host}:18080/reports/{report_id})
```

文件服务器与 Forecast 项目类似，本地启动 HTTP 服务提供静态文件访问。

---

## 8. 输出规范

### 8.1 单事件回答格式示例

```markdown
1. **2025-07-15 14:32:18**
   - 线路：泰吴线
   - 电压等级：500kV
   - 故障杆塔：106号
   - 故障类型：雷击
   - 概述：泰吴线106号塔遭受雷击，保护动作跳闸，重合闸成功。
   - 来源：[国网江苏电力500kV泰吴线2025年7月15日雷击故障分析报告.pdf](http://localhost:18080/reports/{report_id})
```

### 8.2 多设备统计格式示例

```markdown
2025年湖北500kV线路雷击故障统计：

1. **泰吴线**：2 次
   - 2025-07-15 14:32:18，106号塔
   - 2025-08-03 09:15:42，118号塔

2. **武宗Ⅰ线**：1 次
   - 2025-06-30 20:45:10，203号塔
```

---

## 9. 技术栈

- 语言：Python 3.12
- 虚拟环境：`env_fault`（位于项目目录内）
- PDF 解析：pdfplumber 0.11.9
- DOCX 解析：python-docx
- DOC/WPS 解析：LibreOffice（soffice 命令行）
- 向量数据库：Qdrant 本地文件模式
  - 客户端：qdrant-client
  - 存储路径：`./qdrant_db/`
- HTTP 请求：requests
- Embedding：bge-m3（OpenAI Compatible）
  - Endpoint：`http://172.18.179.2:20013/v1/embeddings`
  - 维度：1024
- 大模型：gemma-4-31B-it（OpenAI Compatible）
  - Endpoint：`http://172.18.179.2:20017/v1/chat/completions`
- 前端：Streamlit 或 Gradio（简单测试界面）
- 文件服务器：Python http.server 或 FastAPI 静态文件服务
- HTTP API（对外服务化）：FastAPI 0.141.1 + uvicorn 0.52.1，端口 8503（2026-08-11 上线第一版：`POST /query` 单轮完整答案，供 PLD 等外部项目后端调用；调用方超时建议 ≥300s 与 LLM_TIMEOUT 对齐；反问多轮、SSE 流式、CORS 明确不做）

---

## 10. 项目目录结构

```
/Users/yfzx/Desktop/故障知识库/
├── 故障报告/                      # 285 份原始报告
│   ├── ...
├── env_fault/                     # Python 虚拟环境
├── qdrant_db/                     # Qdrant 本地向量库
├── Project_Skills/                # Skill 目录
│   ├── prompt-skills/
│   │   ├── single_line_stats/
│   │   │   ├── SKILL.md
│   │   │   ├── prompt.txt
│   │   │   └── examples.txt
│   │   ├── single_tower_stats/
│   │   ├── multi_line_stats/
│   │   ├── ranking_stats/
│   │   ├── compare_stats/
│   │   ├── fault_type_stats/
│   │   └── default/
│   └── test/
│       └── scripts/
├── 故障报告_WPS备份/              # WPS 原件备份（转换后移入，不参与索引）
├── logs/                          # 运行日志（含 qa.jsonl 问答日志、回归报告）
├── scripts/                       # 工具脚本（运维脚本按惯例独立顶层）
│   ├── reindex.py                 # 全量/增量重建索引
│   ├── convert_wps.py             # WPS 转 DOCX 一次性转换
│   ├── extract_event_cards.py     # 事件卡批量提取
│   ├── regression.py              # 端到端回归验收（7 用例结构化断言）
│   ├── local_stats.py             # 本地统计验证（无 API，Qdrant 直查）
│   └── migrate_line_names.py      # 线路名归一 payload 迁移（免重嵌入）
├── tests/                         # 测试代码（按惯例独立顶层）
│   ├── test_parser.py             # 解析抽样测试
│   ├── test_metadata.py           # 元数据提取测试
│   ├── test_chunker.py
│   ├── test_vector_store.py
│   ├── test_index.py
│   ├── test_retriever.py
│   ├── test_routing.py
│   ├── test_generator.py
│   ├── test_core_skills.py        # 核心 Skill 问答测试
│   ├── test_end_to_end.py
│   ├── test_ranking.py
│   ├── test_compare.py
│   └── test_fault_type.py
├── src/                           # 系统组件源码目录
│   ├── config.py
│   ├── core/
│   │   ├── parser.py
│   │   ├── metadata.py
│   │   ├── chunker.py
│   │   ├── event_card.py           # 事件卡 LLM 抽取 + 正文时间优先覆盖
│   │   ├── embedding.py
│   │   ├── vector_store.py
│   │   ├── engine.py
│   │   ├── query_parser.py
│   │   ├── retriever.py
│   │   ├── postprocess.py
│   │   ├── qa_logger.py            # logs/qa.jsonl 问答日志
│   │   ├── router_adapter.py
│   │   └── generator.py
│   ├── router/
│   │   └── semantic_router.py
│   ├── file_server/
│   │   └── server.py              # 文件服务器（系统组件）
│   └── interface/
│       ├── app.py                 # Streamlit 前端界面
│       └── api.py                 # FastAPI 对外 HTTP API（端口 8503，POST /query）
├── requirements.md                # 本文件
└── task.md                        # 任务执行单
```

---

## 11. 约束与注意事项

### 11.1 数据约束

- 一个报告对应一个故障事件
- 同一天 + 同一线路 + 同类型 + 同杆塔 = 一次事件
- 同一事件内多次跳闸需保留每次时间

### 11.2 查询约束

- 季度/月份查询必须带年份
- **带线路名的查询不带任何时间信息时，系统反问**"想查询哪个时间段：1. 具体某一年 2. 历年全部"，不再默认某一年；用户回复"历年"或具体年份后由前端拼接原问题重新查询（`_check_missing_time` + 前端 clarify_origin 多轮衔接）
- 杆塔号查询必须带线路名
- 【待办】无线路名的多设备/归纳/通用查询不带年份时暂保留"默认 2026 年"旧逻辑，待优化对应功能类别时评估是否同样改为反问

### 11.3 输出约束

- 电压统一为 `数字kV` 格式
- 省份统一到省级
- 每条事件必须标注来源文件超链接
- 缺失杆塔号需标注

### 11.4 安全约束

- 不泄露 secrets（API key、token、credentials）
- 不在源码中硬编码外部端点密钥
- 临时分析文件使用后删除

### 11.5 开发约束

- 系统组件源码必须位于 `/Users/yfzx/Desktop/故障知识库/src/`（core、router、interface、file_server）
- `tests/`（测试）与 `scripts/`（运维脚本）按 Python 社区惯例独立顶层，不属于系统组件
- 所有代码按 `skills/coding-protocol.md` 执行
- 影响范围 > 3 个文件时触发 `skills/ulw-loop.md`
- 每轮结束必须有验证日志

---

## 12. 已知风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| DOC/WPS 解析失败 | 数据丢失 | 使用 LibreOffice 预处理 |
| 杆塔号提取不全 | 杆塔级查询召回差 | 正则覆盖多种表述 + 后处理高亮 |
| 文件名元数据不规范 | 基础标签错误 | 正文二次验证 + 日志记录异常 |
| LLM 编造时间/杆塔号 | 答案不准确 | 检索层严格过滤 + Prompt 要求只基于上下文 |
| 同一线路多名称 | 检索遗漏 | 建立线路别名索引 |
| 文件服务器未启动 | 超链接失效 | 查询时检测并提示 |
| 重复报告（同一事件多份文件） | 【精确统计】计数虚高 | 已知问题暂不处理（2026-07-28 用户决策）：全库 11 组重复（千伏/kV 变体、批次号、双格式、两省上报）；单设备回答无异常（LLM 合并并标注来源）；备查方案为按（日期+线路+类型+杆塔）事件键去重 |

---

## 13. 后续扩展预留

- v2：支持更多故障类型自动识别
- v3：接入实时故障通报数据
- v4：MCP 服务化对外提供 API（HTTP API 第一版已于 2026-08-11 先行上线，见 §9 HTTP API）

当前架构需在 `interface/` 层保持接口分离，`core/` 层支持 tools 注册机制，为后续扩展预留接入点。
